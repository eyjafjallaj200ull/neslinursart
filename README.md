Description of app behaviour
user is on the home page
Nav is rendered
In nav, the links are rendered
when you click the portfolio, the gallery appears but it's async
gallery component is also async and renders gallery images and lightbox
gallery image should render an image and its description and year
lightbox renders a dialog that's closed
if there is an image id the dialog should open
when the dialog is open it should render prev, next, close buttons and image
when next or prev buttons are clicked the image alt changes because image changes
when close button is clicked the dialog should close